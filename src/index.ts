import { calculateSalary } from "./functions";
import { calculateTax } from "./tax";

// hover over the calculateSalary will show JSDOC descriptions
// console.log(calculateSalary(100));

console.log(calculateTax());
